<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project4n.healthpredictor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash"></activity>
        <activity android:name=".HealthPredictMenu" />
        <activity android:name=".DashBoard" />
        <activity android:name=".otp_View" />
        <activity android:name=".Registration">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login_Screen" />
        <activity android:name=".Login_Reg_Screen" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>